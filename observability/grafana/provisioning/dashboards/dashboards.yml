apiVersion: 1

# -----------------------------------------------------------------------------
# Automatically load dashboards from /var/lib/grafana/dashboards
# -----------------------------------------------------------------------------

providers:
  - name: "IncidentOps Dashboards"
    orgId: 1
    folder: "IncidentOps Arena"
    type: file
    disableDeletion: false
    editable: false
    options:
      path: /var/lib/grafana/dashboards
